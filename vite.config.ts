import { defineConfig } from "vite";

export default defineConfig({
  base: "/firestore-crud-frontend/", // Reemplaza 'nombre-repo' por el nombre de tu repositorio
  build: {
    outDir: "dist",
  },
});
